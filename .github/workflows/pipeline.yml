name: Pipeline
on:
  workflow_dispatch:
    inputs:
      text_in:
        default: 'John John'
        required: false
        type: string
      number_in:
        default: 4
        required: false
        type: number
      

jobs:
  step1:
    uses: ./.github/workflows/step1.yml
    with:
      text_in: ${{ inputs.text_in }}
      number_in: ${{ inputs.number_in }}
